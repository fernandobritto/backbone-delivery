const path = require('path')

module.exports = {
    config: path.resolve(__dirname, 'source', 'config', 'database.ts'),
    'migrations-path': path.resolve(__dirname, 'source', 'database', 'migrations')
}
